import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Tags',
};

export default function TagsPage() {
    return (
        <div>
            <h1>Tags Page</h1>
        </div>
    )
}